import { expect, test } from "vitest";
import { loadCodeContribution } from "./load";

test("loadCodeContribution", () => {
  // TODO: replace smoke test by unit test
  expect(loadCodeContribution).toBeInstanceOf(Function);
});
