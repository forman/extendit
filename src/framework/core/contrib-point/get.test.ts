import { expect, test } from "vitest";
import {
  getContributions,
  getContributionPoint,
  getContributionPoints,
  getExtensionContributions,
} from "./get";

test("getContributions", async () => {
  // TODO: replace smoke test by unit test
  expect(getContributions).toBeInstanceOf(Object);
});

test("getContributionPoint", async () => {
  // TODO: replace smoke test by unit test
  expect(getContributionPoint).toBeInstanceOf(Object);
});

test("getContributionPoints", async () => {
  // TODO: replace smoke test by unit test
  expect(getContributionPoints).toBeInstanceOf(Object);
});

test("getExtensionContributions", async () => {
  // TODO: replace smoke test by unit test
  expect(getExtensionContributions).toBeInstanceOf(Object);
});
